<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fragmentexample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DemoActivity"></activity>
        <activity android:name=".StudentInfomationActivity" />
        <activity android:name=".XuLyGiaoDienActivity" />
        <activity android:name=".DialogFragmentActivity" />
        <activity android:name=".ListFragmentActivity" />
        <activity android:name=".RemoveAndPopBackStackActivity" />
        <activity android:name=".FragmentTruyenNhanDuLieuActivity" />
        <activity android:name=".GiaoTiepFragmentActivity" />
        <activity android:name=".AddFragmentActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>